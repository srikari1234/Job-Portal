# Code style

The backend uses the corrected project structure and API behavior from the supplied reference project. Source files contain no explanatory code comments. The implementation keeps the logic in controllers, validation in Joi schemas, database rules in Mongoose models, and access control in middleware.
